export default function cards() {
  const cards = document.querySelector(".card");
  const deckOfCards = cards.className.split(" ");

  function shuffleCards(){
      deckOfCards.forEach(name =>{
      onclick()
      })
  }
}
  
